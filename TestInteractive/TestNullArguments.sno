      pop()
      Pop(,)
      POP(,3.14)
      pop(0,)
      pop(2,3)
      pop(,,)
      pop(,e,)
      pop(a,,b)
      pop(1)
      pop(1,2)
      pop(1 * 2,2 ^ 4)
      ($("S" "004"))(1 * 2,3 / 5) 
